# @blinkbitcoin packages are published to GitHub Packages. The read:packages token
# never belongs here: developers keep it in their own ~/.npmrc and CI takes it from
# the workflow environment.
@blinkbitcoin:registry=https://npm.pkg.github.com
